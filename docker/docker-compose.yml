version: "3.9"

# ══════════════════════════════════════════════════════════════════════════════
#  hostkita Tunnel — Production docker-compose
#  Usage:
#    cp ../.env.example ../.env   # lalu edit ROOT_PASS & NTFY_TOPIC
#    docker compose -f docker/docker-compose.yml up -d
# ══════════════════════════════════════════════════════════════════════════════

services:
  hostkita-tunnel:
    image: hostkita-tunnel:latest
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        BORE_VERSION: "0.6.0"
        TZ: "${TZ:-Asia/Jakarta}"
    container_name: hostkita-tunnel
    hostname: hostkita-tunnel
    restart: unless-stopped

    environment:
      - ROOT_PASS=${ROOT_PASS:-ChangeMe123!}
      - NTFY_TOPIC=${NTFY_TOPIC:-hostkita-mail}
      - PORTS=${PORTS:-22}
      - BORE_SERVER=${BORE_SERVER:-66.33.22.220}
      - PORT=${PORT:-8080}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - TZ=${TZ:-Asia/Jakarta}

    ports:
      - "${PORT:-8080}:${PORT:-8080}"

    volumes:
      # Persist Supervisor and service logs across container restarts
      - hostkita-logs:/var/log/supervisor
      - hostkita-tunnel-logs:/var/log/hostkita-tunnel
      # Persist SSH host keys so clients don't see key-changed warnings on restart
      - hostkita-ssh-keys:/etc/ssh

    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:${PORT:-8080}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  hostkita-logs:
    name: hostkita-tunnel-supervisor-logs
  hostkita-tunnel-logs:
    name: hostkita-tunnel-service-logs
  hostkita-ssh-keys:
    name: hostkita-tunnel-ssh-keys
